/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.flatfile.api.resources.auth.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class Credentials {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private Credentials(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static Credentials userCredentials(UserCredentials value) {
        return new Credentials(new UserCredentialsValue(value));
    }

    public static Credentials apiCredentials(ApiCredentials value) {
        return new Credentials(new ApiCredentialsValue(value));
    }

    public boolean isUserCredentials() {
        return value instanceof UserCredentialsValue;
    }

    public boolean isApiCredentials() {
        return value instanceof ApiCredentialsValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<UserCredentials> getUserCredentials() {
        if (isUserCredentials()) {
            return Optional.of(((UserCredentialsValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ApiCredentials> getApiCredentials() {
        if (isApiCredentials()) {
            return Optional.of(((ApiCredentialsValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitUserCredentials(UserCredentials userCredentials);

        T visitApiCredentials(ApiCredentials apiCredentials);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(UserCredentialsValue.class), @JsonSubTypes.Type(ApiCredentialsValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("userCredentials")
    private static final class UserCredentialsValue implements Value {
        @JsonUnwrapped
        private UserCredentials value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private UserCredentialsValue() {}

        private UserCredentialsValue(UserCredentials value) {
            this.value = value;
        }

        @Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitUserCredentials(value);
        }

        @Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof UserCredentialsValue && equalTo((UserCredentialsValue) other);
        }

        private boolean equalTo(UserCredentialsValue other) {
            return value.equals(other.value);
        }

        @Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @Override
        public String toString() {
            return "Credentials{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("apiCredentials")
    private static final class ApiCredentialsValue implements Value {
        @JsonUnwrapped
        private ApiCredentials value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ApiCredentialsValue() {}

        private ApiCredentialsValue(ApiCredentials value) {
            this.value = value;
        }

        @Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitApiCredentials(value);
        }

        @Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ApiCredentialsValue && equalTo((ApiCredentialsValue) other);
        }

        private boolean equalTo(ApiCredentialsValue other) {
            return value.equals(other.value);
        }

        @Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @Override
        public String toString() {
            return "Credentials{" + "value: " + value + "}";
        }
    }

    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @Override
        public String toString() {
            return "Credentials{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
